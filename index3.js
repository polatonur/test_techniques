const tellMeYa = (arr, number) => {
  let counter = 0;

  //   find all combinations
  for (let i = 0; i < arr.length - 1; i++) {
    for (let j = i + 1; j < arr.length; j++) {}
  }

  // check wheter total is equal to number
  const calculateTotal = (arr) => {
    let total = 0;
    for (let i = 0; i < array.length; i++) {
      total += i;
    }
  };
  return total;
};
